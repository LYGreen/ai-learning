# Basic terminal Commands

- pwd = path of the current directory.
- ls = list //it shows you all the files and directory in your current folder.
- cd .. = up one level in the directory structure.
- cd directory/file path = moves you into that directory.
